ls /sys/class/net/  ==> list int names

#!/bin/bash

arr="$(ls /sys/class/net/)"
#IFS='\n' 
#read -ra array <<< $arr
#echo ${array[1]}
#for val in "${array[@]}";
#do
# echo "$val"
#done
readarray -d $'\n' -t newarr <<< "$arr"
echo ${newarr[1]}

for ip in ${newarr[@]};
do 
test=$(ifconfig $ip | grep -o -P '(?<=inet ).*(?= netmask)')
echo $test
done
